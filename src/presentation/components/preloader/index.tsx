import React, { FC } from 'react'
import { StyledLoader } from './style'

const Preloader: FC = () => {
  return (
        <StyledLoader>
            loading......
        </StyledLoader>
  )
}

export default Preloader
